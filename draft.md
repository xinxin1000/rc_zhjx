有一个配置系统，记录某个事件对应的外部api的地址、header、body
这个服务本身读取这个配置，定时更新，同时暴露接口，调用时传入事件的唯一标识，然后调用配置的对应api
存储每次事件+api返回值+关键字段的值，有一个对应平台页面可以查询
api 返回code!=0 记录完整请求，重试3次
还是失败，投入消息队列衰减调用api
最终还是失败，死信队列+发送一条微信消息

降级策略
不用发送消息队列，直接不重试

感觉应该配合一个接口，输入一个比如开启降级的时间范围，然后扫描这个时间范围内的投递失败的record,重新投递

saga
cel